# Variáveis de Ambiente para a API

# -- Configurações do Servidor --
# Host e porta em que a API será executada.
API_HOST=0.0.0.0
API_PORT=8000
# Ativar o recarregamento automático do servidor em caso de alterações no código.
API_RELOAD=True
# Ativar modo de depuração (pode expor informações sensíveis).
API_DEBUG=True

# -- Segurança --
# Token estático para autenticação Bearer. Troque por um valor seguro e aleatório.
# Exemplo de como gerar um no terminal: openssl rand -hex 32
STATIC_BEARER_TOKEN="seu_token_secreto_aqui"

# -- Banco de Dados --
# Caminho para o arquivo do banco de dados SQLite.
# O caminho deve ser relativo à raiz do projeto (onde você executa o uvicorn).
DATABASE_URL="sqlite:///data/rfb_empty.db"

# -- SSL/TLS (Opcional) --
# Caminhos para os arquivos de chave e certificado SSL para habilitar HTTPS.
# Deixe em branco para rodar em HTTP.
# Em produção, é recomendado usar um proxy reverso (Nginx, Traefik) para lidar com SSL.
SSL_KEYFILE_PATH=
SSL_CERTFILE_PATH=